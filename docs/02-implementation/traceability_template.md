# 要求トレーサビリティ

設計ID → 受け入れ条件 → 実装 → テストの対応が一列で辿れる状態を保つ。
実装タスク完了ごとに更新する（`task-worker` の責務）。

| 設計ID | 受け入れ条件ID | タスク | 実装ファイル | テスト（ファイル::テストID） | 状態 |
|---|---|---|---|---|---|
| DD-001 | AC-001 | T-001 | TODO | TODO | 未着手 |

状態: `未着手` / `実装中` / `テスト済` / `独立レビュー済`

- 受け入れ条件に対応するテストが1つもない行を作らない。
- テストが実装の写経になっていないかは独立レビュー（reviewer）で確認される。
